doctype html
html
    head
        meta(charset = 'utf-8')
        meta(name='google-signin-client_id' content='516803201821-k6ejfn3u11qkr89akbjtraivqenbue2m.apps.googleusercontent.com')
        meta(name='google-signin-scope' content='profile email')
        script(src='//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js')
        script(src="https://apis.google.com/js/platform.js?onload=init" async defer)
        script(type='text/javascript' src='js/common.js') 
        script.
            function signIn(googleUser) {
                alert('test')
                var profile = googleUser.getBasicProfile();
                var data = {
                    token : googleUser.getAuthResponse().id_token,
                    id : profile.getId(),
                    email : profile.getEmail(),
                    name : profile.getName()
                }
                $.ajax({
                    url: '/confirm',
                    dataType: 'json',
                    data: data,
                    type: 'GET',
                    success: function(data) {
                        //alert('success')
                        //window.location ='/list';
                    }
                });
            }
    body
        //- h1 Welcome to My Awesome App
        div(class="g-signin2" data-onsuccess="signIn" data-theme="dark" id='my-signin2')
        button(type='button' onclick='logout()') 로그아웃